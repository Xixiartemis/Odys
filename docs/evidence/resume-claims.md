# Resume Claim Templates

## Current conservative mechanism claim

Built an agent recovery control plane for Odys with durable failure
provenance, no-progress-aware escalation, bounded recovery budgets,
macro-replan execution, receipt-backed side-effect reconciliation, and
external-validator-gated completion; validated the control path with
provider-free regression tests and a scoped six-run real-provider recovery
experiment.

## Future benchmark-backed metric template

Built and evaluated an agent recovery control plane on **[N]** paired
task-condition runs using **[model/provider]** and a shared external
validator; at validator-equivalent outcomes, Odys changed median recovery
cost by **[X% calls] / [Y% tokens] / [Z% wall time]**, with **[A/B]**
fault-conditioned verified completions. Replace every placeholder only after
the Phase 5 artifacts contain the corresponding raw, trace, validator, and
pair-level evidence.
